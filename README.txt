# Othello bot

This project does not require any exotic dependency, so a basic python environment is enough to run it.

To start the project, run the following command in the project root folder:

python main.py

